pipeline {
    agent { label 'demo' } // Run on your "demo" node
    
    stages {
        stage('Checkout Code') {
            steps {
                git url: 'https://github.com/vaibhavkolase20/jenkins_Tool.git', branch: 'main'
            }
        }

        stage('Build') {
            steps {
                echo 'Build stage: No actual build, just testing Jenkins pipeline.'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deployment stage: No real deployment, just testing pipeline flow.'
            }
        }
    }
}
